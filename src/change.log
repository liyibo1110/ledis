0.1变化
新的配置：
1.修改配置timeout，如果是0则无时间限制。
2.增加dbfilename配置。
3.增加maxclients最大连接数配置。
新功能：
1.增加了新的命令smove/sunion/sunionstore/sdiff/sdiffstore/getSet/ttl/slaveof
2.所有的log输出，增加了当前时间前缀。
修改BUG：无
代码调整：
1.list结构增加了内部的迭代器，同时增加了配置rewind/rewindTail/yield操作函数。
2.anet的backlog参数由32提高到64。
3.规范了dict的参数（unsigned int改成了unsigned long），也新引用了limits.h的极值常量。
4.ledis中客户端参数argv容器，由16个固定长度，改成了动态长度，初始值为4个。
5.del命令支持传多个key了，而不是原来的1个。
6.emptyDb函数，会返回清空前所有库节点的总数。
7.incrby和decrby的增减量，由int改成long long，范围大多了。

0.96变化
修改BUG：
1.ledis中readQueryFromClient函数存在超过16个参数则跳过free的情况，这属于严重BUG。

0.95变化
修改BUG：
1.ledis中ldbLoadLzfStringObject函数忘了zfree了，这属于严重BUG。

0.94变化
代码调整：
1.ledis中对dict桶数和节点数的变量类型由int改成了long long（但是dict里面本身结构却没改）

0.93变化
修改BUG：
1.ledis中ldbSaveLzfStringObject函数，为压缩后缓冲区多分配1个字节（应该是存\0），这并不会每次都有问题。

0.92变化
修改BUG：
1.dict中dictGetRandomKey函数，已用节点数为0则直接返回NULL，而不是桶数为空，桶数永远不为空，这属于严重BUG。

0.91变化
代码调整：
1.sinterGenericCommand函数，允许其中参数key找不到，如果是sinterstore则会返回ok，否则返回*-1而不是nokeyerr的错误字符串。

beta-9变化
新的配置：
1.增加requirepass配置，即设置连接密码，正确才能执行其它命令。
2.增加shareobjects配置，默认是no，即之前版本的obj池子复用，原来是强制复用。
新功能：
1.持久化LDB文件格式发生了巨大变化，几乎是重做，还使用了lzh字符串压缩算法。
2.增加expire过期机制，所有dict相关操作都发生了变化（像是半成品，expire需要用专门的command来设置，而且很多command都会直接干掉设置了的key）
修改BUG：无
代码调整：
1.Makefile的编译指令，指定c99为当前标准，还限制了必须用ANSI C的标准。
2.封装了新头文件fmacros.h，开启了_BSD_SOURCE和_XOPEN_SOURCE特性开关，并在各个文件按需引入。
3.anet中read和write函数，ptr参数由void*改成了更明确的char*。
4.zmalloc里所有返回的指针，均被显式转型成char*，但函数返回类型还是void*。
5.sds.h的sdshdr中buf初始由buf[0]改成了buf[]，sds.c的sdssplitlen函数有了一个很微小的改动。
6.dict的获取槽数和已用节点数的宏名，改成了更简洁的dictSlots和dictSize。
7.server和client结构中dict变成了ledisDb结构，封装了一层。
8.增加了对key实际值访问的函数（同时也处理expire的情况），因此几乎所有command都发生了调整。
9.shared静态字符串对象有了很大变动。

beta-8变化
新的配置：
1.增加pidfile配置，配合daemonize配置，可以指定pid文件生成的位置。
新功能：
1.增加zmalloc文件，此版本只是额外可以追踪内存申请和释放动作，从而统计堆内存使用量，内部实现还是malloc/free。
2.增加了全新的cli客户端程序，但只可以自动连接并执行单条命令，并没有模拟终端交互功能。
3.增加了新的MONITOR客户端类型。
4.增加了mget命令。
5.去掉了version命令，换成了信息量更大的info命令。
修改BUG：无
代码调整：
1.在所有程序调用malloc/free/realloc时，均替换成了zmalloc/zfree/zrealloc函数。
2.增加了obj池子的最大数目限制（10000提高到1000000）。
3.Makefile随之更新，但里面有sha1.c和sha1.h，但实际没对应文件，并且zmalloc.c也没有引入h文件。

beta-7变化
新的配置：
1.增加daemonize配置，即后台运行，默认是no（如果开启，会生成/var/log/ledis.pid文件），logfile情况也跟着发生了一些变化。
新功能：
1.开始明确增加了当前版本号。
2.增加了新命令sinterstore，和sinter类似，只是将结果存到指定的新key中。
3.增加了flushdb和flushall命令，只是简单的清空当前库或者所有库。
4.增加了lrem命令，可以删除一组LIST结构里的特定值元素。
5.增加了version命令，简单输出当前版本号。
6.增加了sort复杂第一版命令，但功能并不完整。
修改BUG：
1.list结构的len，由int改成了unsigned int。
代码调整：
1.在所有程序文件顶部，更新了新版的版权说明信息。
2.benchmark增加了PING命令的测试。

beta-6变化
新的配置：
1.增加slaveof，即从服务器可以配置主服务器的ip和port。
2.增加glueoutputbuf，即合并推送要返回client的消息，默认开启。
新功能：
1.主从复制。（原理是一开始从直接复制主的dump文件，然后当主进行了修改后，模拟TCP-Client给所有的从，发相同的command，让从自己再次执行，不太可靠）
2.benchmark性能测试程序。
修改BUG：无，但基本都随着command的修改都改了一遍。
代码调整：
1.anet增加了nonblock的client选项，同时也开启了REUSEADDR选项。
2.anetTcpServer的backlog参数由5提高到32。
3.dict增加了dictEmpty函数，即清空并析构指定的dict（内部原来就有实现，只是未公开）。
4.ae增加了aeWait函数，指定fd使用select系统调用查看是否就绪，并可以传入timeout。
5.ledis增加了hash新类型，增加了obj池子的最大数目限制。
6.client结构的argv字段，类型由字符串数组，改成obj指针的数组。
7.精简了hash相关实现，无论是key还是val，以后都是obj形式封装，不会再直接是sds。
8.针对上一条，修改了所有的command函数的细节，主要是dict的key/val以及返回reply。

beta-5变化
代码调整：
1.只修改了sdscatprintf函数的va_start/end的位置（每轮尝试扩展都会重新生成）

beta-4变化
新的配置：
1.增加了port配置（默认6379）
2.增加了bind配置（默认通配所有）
新功能：无
修改BUG：
1.修改了setnx命令，如果已有值，则不会增加dirty值。
2.如果del不成功，则返回zero而不是ok。
3.initServer函数放到加载conf文件之后再进行，否则dbnum永远都是16个默认。
代码调整：
1.为各种类型的错误err封装成了obj，不再写死字面量。
2.修改所有带nx参数的命令，如果不存在，成功操作统一返回one，已存在则返回zero之类的信息，而不是明显的错误文字。
3.setnx命令，如果已有值，返回zero而不是返回ok。
4.move命令如果没有key或者目标已存在，返回zero而不是错误，如果成功返回one而不是ok。
5.sadd命令成功返回one，已存在则返回zero，而不是原来的ok。
6.srem命令不存在key则返回zero，而不是错误信息，成功删除返回one，没找到元素则返回zero，而不是ok。
7.sinter和smembers命令，如果参数key不存在，则退出并返回nil，而不是错误信息。

beta-3变化（主文件只有redis.c发生了变化，增加了新的命令）
1.增加了自己的Changelog文件。
2.修改了randomKey的BUG。
3.修改了如果saveDb失败，不会删除临时文件的BUG。
4.增加了新的数据类型set，本质是只有key没有val的dict。
5.增加了type，lset，sadd，srem，scard,sismember命令。
6.增加了针对新set类型的save/load配套相关功能。

beta-2变化
1.增加了基于ruby的演示客户端
2.修改了部分README文字
3.修改了saveDb和loadDb，对所有val的sds类型加入了长度非0判断，即sdslen返回0则只写长度为0,字符串不写入文件，读取的时候也是还原一个sdslen为0的sds结构